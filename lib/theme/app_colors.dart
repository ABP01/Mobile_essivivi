import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFF742F);
  static const Color background = Color(0xFFFBFBFB);
  static const Color darkBackground = Color(0xFF292929); // For tracking screen
  static const Color cardOverview = Color(0xFF292929); // For map dark elements
  
  static const Color textMain = Color(0xFF191919);
  static const Color textSecondary = Color(0xFF999999);
  
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
